<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:splitMotionEvents">false</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
    </style>

    <!-- 应用启动页(StartingWindow)的theme -->
    <style name="StartingWindowTheme" parent="AppTheme">
        <!--也可以设置成一张图片 -->
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>

    <style name="selectTextView">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#252430</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="unSelectTextView" >
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#4D4B5E</item>
        <item name="android:textStyle">normal</item>
    </style>
</resources>